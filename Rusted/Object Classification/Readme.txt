Training: 
Extract ORB features of a training image dataset, then run k-means clustering on them and use that to create a bag of visual words histogram for each image category. 
Classification:
Use K-Nearest Neighbor classifier to classify any new images based on what was learned from the training dataset.